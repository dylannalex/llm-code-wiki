# Contracts — index

Service-to-service interfaces / data boundaries (how A exposes data for B to consume).
One line per page. (Optional category — remove if your domain isn't multi-service.)

| page | scope | summary |
|------|-------|---------|
| _none yet_ | | |
